let employeeName = "ezra";
let employeeJobTitle = "teacher"
let employeePayRate = 19.99;

function DisplayEmployeeInfo(name, job, payRate){
    console.log("The employee name ${name} works ad a ${jobTitle} and makes  ${payRate} +".")
    )
}

DisplayEmployeeInfo(employeeName, employeeJobTitle, employeePayRate);



let employee = {
    name: "Matt",
    jobTitle: "Instructor,
    payRate: 19.99
}

function displayEmployeeInfo2(employee){
    console .log('The employee named ${employee.name} works as a ${employee.jobTitle} and makes ${payRate} + "."')
}